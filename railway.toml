[build]
builder = "nixpacks"
buildCommand = "npx prisma generate --schema=packages/database/prisma/schema.prisma && npm run build --workspace=apps/bot"

[deploy]
startCommand = "npx prisma migrate deploy --schema=packages/database/prisma/schema.prisma && node apps/bot/dist/index.js"
restartPolicyType = "always"
