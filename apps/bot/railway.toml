[build]
builder = "nixpacks"
buildCommand = "npm install --include=dev && npm run db:generate --workspace=packages/database && npm run build --workspace=apps/bot"

[deploy]
startCommand = "node apps/bot/dist/index.js"
restartPolicyType = "always"
