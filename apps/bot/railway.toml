[build]
builder = "nixpacks"
buildCommand = "npm install && cd packages/database && npm install && npx prisma generate && cd ../.. && npm run build --workspace=apps/bot"

[deploy]
startCommand = "node apps/bot/dist/index.js"
restartPolicyType = "always"
